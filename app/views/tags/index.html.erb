<h1>All Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tags_path(tag) %>
    </li>
  <% end %>
</ul>

<%= link_to "Return", articles_path %>
<%= link_to "Tags", tags_path %>
